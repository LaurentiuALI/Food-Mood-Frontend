import React from 'react'
import ProtectedRoute from '../../common/protected-route/ProtectedRoute'

const Preferences = ({title}) => {
  return (
    <div>
        <h1>{title}</h1>
    </div>
  )
}

export default Preferences